<script setup lang="ts">
defineProps<{
  title: string
  description?: string
  actionLabel?: string
}>()
defineEmits<{ (e: 'action'): void }>()
</script>

<template>
  <div class="flex flex-col items-center justify-center py-16 px-4 text-center">
    <div class="w-16 h-16 rounded-tide-card bg-gradient-to-br from-[rgba(90,138,171,0.16)] to-[rgba(139,124,168,0.16)] flex items-center justify-center mb-4">
      <svg class="w-8 h-8 text-tide-blue-deep" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
          d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
      </svg>
    </div>
    <h3 class="font-display text-xl font-normal text-tide-ink mb-1">{{ title }}</h3>
    <p v-if="description" class="text-sm text-tide-ink/60 mb-4 max-w-xs">{{ description }}</p>
    <button
      v-if="actionLabel"
      @click="$emit('action')"
      class="inline-flex items-center gap-2 px-4 py-2 bg-gradient-to-br from-tide-blue-btn to-tide-blue-btn-deep text-tide-paper text-sm font-medium rounded-tide-btn hover:brightness-105 transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-tide-blue focus-visible:ring-offset-2"
    >{{ actionLabel }}</button>
  </div>
</template>
